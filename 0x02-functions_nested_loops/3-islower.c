#include "main.h"

/**
 * _islower - Entry point
 * @c - input letter
 * Description - it checks if an input is a lowercase
 * Return 1 if true an 0 if not
 */

int _islower(int c)
{

	int c;

	if (_islower(c) != 0)
	{
		return (1);
	}
	else
	{
		return (0);
	}

}
