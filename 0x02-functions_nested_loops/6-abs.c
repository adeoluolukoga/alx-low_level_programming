#include "main.h"

/**
 * _abs - converts a number to absolute value
 * @int: The input number
 *
 * Description - absolute value.
 *
 * Return: int, 0 or -int.
 */

int _abs(int)
{

	if (int < 0)
	{
		return (int * (-1));
	}
	else if (int == 0)
	{
		return (0);
	}
	else
	{
		return (int);
	}

}
